import { initSettings } from '../../../../test/tools/initSettings';

describe('Create-user-use-case', () => {
  beforeAll(async () => {
    const settings = await initSettings();
  });

  it('', () => {
    expect(1).toBe(1);
  });
});
