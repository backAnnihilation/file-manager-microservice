export enum RoutingEnum {
  comments = '/comments',
  blogs = '/blogs',
  posts = '/posts',
  admins = '/sa/users',
  auth = '/auth',
  security = '/security/devices',
}

export enum ApiTagsEnum {
  Users = 'users',
  Auth = 'auth',
  Security = 'security',
}
