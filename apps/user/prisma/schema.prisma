

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = "postgres://avnadmin:AVNS_C_w6bSq7j6bueOavIug@pg-ebf4a9a-commonbackmail-5fad.f.aivencloud.com:11550/defaultdb?sslmode=require"
}

model UserAccount {
  id                           String         @id @default(uuid())
  userName                     String         @unique
  email                        String         @unique
  passwordHash                 String
  confirmationCode             String
  confirmationExpDate          DateTime
  isConfirmed                  Boolean
  passwordRecoveryCode         String?
  passwordRecoveryExpDate      DateTime?
  createdAt                    DateTime       @default(now())
  updatedAt                    DateTime       @updatedAt

  userSessions                 UserSession[]  @relation("UserSessions")
}

model UserSession {
  id                String         @id @default(uuid())
  ip                String
  userAgentInfo     String         
  deviceId          String         @unique
  refreshToken      String         
  rtIssuedAt        DateTime       
  rtExpirationDate  DateTime       

  userAccount       UserAccount    @relation("UserSessions", fields: [userId], references: [id], onDelete: Cascade)
  userId            String
}