export enum RoutingEnum {
  profiles = '/profiles',
  users = '/users',
  comments = '/comments',
  blogs = '/blogs',
  posts = '/posts',
  admins = '/sa/users',
  auth = '/auth',
  security = '/security/devices',
  base = '/',
}

export enum ApiTagsEnum {
  Users = 'users',
  Auth = 'auth',
  Security = 'security',
  Admin = 'sa',
  Profiles = 'profile',
}
