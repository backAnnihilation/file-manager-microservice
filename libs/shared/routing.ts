export enum RoutingEnum {
  photos = '/photos',
  files = '/files',
  profiles = '/users/profiles',
  users = '/users',
  comments = '/comments',
  blogs = '/blogs',
  posts = '/posts',
  admins = '/sa/users',
  auth = '/auth',
  security = '/security/devices',
  base = '/',
}

export enum ApiTagsEnum {
  Posts = 'posts',
  Users = 'users',
  Profiles = 'users/profiles',
  Auth = 'auth',
  Security = 'security',
  Admin = 'sa',
  Photos = 'photos',
  Files = 'files',
}
